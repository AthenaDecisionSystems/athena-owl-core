apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "decision-svc.fullname" . }}-test-connection"
  labels:
    {{- include "decision-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "decision-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
