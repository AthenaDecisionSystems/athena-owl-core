apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: owl-backend-claim0
  name: owl-backend-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
