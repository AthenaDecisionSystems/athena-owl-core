apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: owl-backend-claim2
  name: owl-backend-claim2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
