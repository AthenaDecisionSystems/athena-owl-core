apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: decisionsvc-claim0
  name: decisionsvc-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
