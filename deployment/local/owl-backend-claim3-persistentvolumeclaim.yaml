apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: owl-backend-claim3
  name: owl-backend-claim3
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
